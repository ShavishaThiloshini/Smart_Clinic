import type { Notification } from '../../types/notification.types';
type Props = { notification: Notification; onMarkAsRead: (notificationId: number) => void };
const icons: Record<string, string> = { appointment_created: '📅', appointment_confirmed: '✓', appointment_cancelled: '✕', appointment_rescheduled: '↻', appointment_completed: '✓', 'appointment_no-show': '!', general: '✉' };
function dateLabel(value: string) { const date = new Date(value); return Number.isNaN(date.getTime()) ? value : date.toLocaleString('en-GB', { day: '2-digit', month: 'short', hour: '2-digit', minute: '2-digit' }); }
export function NotificationItem({ notification, onMarkAsRead }: Props) { return <article className={`notification-item ${notification.isRead ? 'read' : 'unread'}`} style={{ backgroundColor: notification.isRead ? '#fff' : '#f0f7ff', border: `1px solid ${notification.isRead ? '#eaeaea' : '#cce5ff'}`, borderRadius: '10px', padding: '1rem', display: 'grid', gridTemplateColumns: '36px 1fr auto', gap: '0.875rem', alignItems: 'start' }}><span aria-hidden="true" style={{ width: '36px', height: '36px', borderRadius: '50%', display: 'grid', placeItems: 'center', background: notification.isRead ? '#f4f6f8' : '#dbeeff' }}>{icons[notification.type] || icons.general}</span><div><div style={{ display: 'flex', justifyContent: 'space-between', gap: '0.75rem', alignItems: 'baseline' }}><h3 style={{ margin: 0, color: '#24324a', fontSize: '0.95rem', fontWeight: notification.isRead ? 600 : 750 }}>{notification.title}</h3><time style={{ color: '#71809a', fontSize: '0.75rem', whiteSpace: 'nowrap' }}>{dateLabel(notification.createdAt)}</time></div><p style={{ margin: '0.4rem 0 0', color: '#536680', fontSize: '0.88rem', lineHeight: 1.5 }}>{notification.message}</p></div>{!notification.isRead && <button type="button" onClick={() => onMarkAsRead(notification.notificationId)} aria-label={`Mark ${notification.title} as read`} style={{ border: 0, background: 'transparent', color: '#315be7', cursor: 'pointer', fontWeight: 700, fontSize: '0.78rem', padding: '0.25rem' }}>Mark read</button>}</article>; }
